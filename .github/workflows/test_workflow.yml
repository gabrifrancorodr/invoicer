name: Test Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    name: Test2
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      - name: Test App Action
        run: |
          bin/rails test
